type Product = {
  id: number;
  name: string;
  price: number;
  description: string;
  image: string;
};

type products = {
  id: number;
  name: string;
  price: number;
  description: string;
  image: string;
};

export type { Product, products };
